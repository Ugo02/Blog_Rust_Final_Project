// Declare the `auth_handlers` module, which likely handles authentication-related routes (e.g., login, logout, resgister).
pub mod auth_handlers;

// Declare the `post_handlers` module, which likely manages post-related routes (e.g., creating, fetching, updating posts).
pub mod post_handlers;